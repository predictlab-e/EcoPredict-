# EcoPredict

**EcoPredict** — это аналитическая платформа для предсказательных рынков, построенная на Python и Streamlit.  
Она объединяет байесовские модели, калибровку вероятностей, риск‑менеджмент и визуальный интерфейс.

## 🔧 Структура проекта

- `Single.py` — главный оркестратор: загрузка данных, признаки, модели, ансамбли, риск
- `model.py` — алгоритмы: байес, калибровка, декей, CI, ансамбли
- `utils.py` — утилиты: логирование, кэш, статистика, HTTP, сериализация
- `app.py` — Streamlit интерфейс с 10 вкладками
- `requirements.txt` — зависимости
- `Dockerfile` — деплой через Docker

## 🚀 Запуск локально

```bash
pip install -r requirements.txt
streamlit run app.py
